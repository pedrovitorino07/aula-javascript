function mult(num) { //chama a função mult
    for (let i = 1; i <= 10; i++) { //mostra os multiplicadores de 7 e inicia a tabuada até o 10
        console.log(`${i} x ${num} = ${num * i}`) 
    }
}

mult(7);
