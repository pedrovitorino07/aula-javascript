let i = 1; //valor inicial

while (i <= 20) { //numero em que vai parar
    if (i % 2 !== 0) { //verificar se o numero é impar
        console.log(i); // console log valor impar
    }
    i++; // incremeta o proximo numero
}
